---
id: CONTRACT-NNN
title: <Title>
status: current
created: YYYY-MM-DD
updated: YYYY-MM-DD
authors: [<name-or-team>]
drafted_by: <human-or-agent>
scope: [<domain>]
tags: [contract]
depends_on: [<SPEC-id>]
implements: []
related: []
---

# Contract: <Title>

<!-- Primary question: What interface does the implementation expose? -->

## Scope

<!-- Name the interface (HTTP API, events, CLI) and the spec whose behavior it exposes. -->

## Interface

<!-- Endpoints, payloads, events. Use tables or fenced examples. Point `implements` at the machine-readable definition or the code that exposes it. -->

## Errors

<!-- Error codes, their meaning, and their transport mapping (HTTP status, event outcome). -->

## Compatibility

<!-- Versioning, deprecations, and what counts as a breaking change. -->
