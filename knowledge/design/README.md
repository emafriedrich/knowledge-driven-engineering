# Design Knowledge

Design knowledge covers product structure, user movement, and reusable UI rules.

Use separate artifacts for:

- Information Architecture: concepts, screens, sections, hierarchy.
- User Flow: paths and decision points.
- Design System: reusable visual and interaction rules.

Keep feature behavior in specs unless the behavior exists to define a reusable UI rule.

